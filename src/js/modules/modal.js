const modal = () => {

    const bindModal = (triggerModal, selectMod, closeModal) => {

        const triggerBtn = document.querySelectorAll(triggerModal),
            selectModal = document.querySelectorAll(selectMod),
            closeBtn = document.querySelector(closeModal);

        triggerBtn.forEach(btn => {

        });




    };




};
export default modal;